name: .NET Build and GitHub Release

on:
  push:
    branches: [ "master" ]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write  # Needed to create releases

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        configuration: [Release]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Generate changelog since last tag
        id: changelog
        shell: pwsh
        run: |
          git fetch --tags --force
          $lastTag = git tag --sort=-creatordate | Select-Object -First 1
          if ([string]::IsNullOrWhiteSpace($lastTag)) {
            $lastTag = "(none)"
            $changes = "No previous tags found."
          } else {
            $changes = git log "$lastTag..HEAD" --pretty=format:"* %s (%h)"
            if ([string]::IsNullOrWhiteSpace($changes)) {
              $changes = "No changes since last tag."
            }
          }

          "last_tag=$lastTag" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "changes<<EOF" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          $changes | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "EOF" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Restore dependencies
        run: dotnet restore StarResonanceDpsAnalysis.slnx

      - name: Build solution
        run: dotnet build StarResonanceDpsAnalysis.slnx --configuration ${{ matrix.configuration }} --no-restore

      - name: Run tests
        run: dotnet test StarResonanceDpsAnalysis.slnx --configuration ${{ matrix.configuration }} --no-build

      - name: Publish project
        run: dotnet publish StarResonanceDpsAnalysis.WPF/StarResonanceDpsAnalysis.WPF.csproj -c ${{ matrix.configuration }} -o publish -f net8.0-windows

      - name: Verify publish output
        run: dir publish

      - name: Create ZIP package
        run: Compress-Archive -Path publish/* -DestinationPath StarResonanceDpsAnalysis-${{ github.run_number }}.zip

      - name: Verify ZIP file
        run: dir StarResonanceDpsAnalysis-${{ github.run_number }}.zip

      - name: Get latest commit info
        id: commit
        shell: pwsh
        run: |
          $commit = git log -1 --pretty=format:"%s"
          $author = git log -1 --pretty=format:"%an"
          $body = git log -1 --pretty=format:"%b"
      
          echo "message=$commit" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          echo "author=$author" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          echo "body=$body" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')  
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          prerelease: true
          body: |
            ✅ Automated Release from GitHub Actions
            • Build: ${{ github.run_number }}
            • Tag: ${{ github.ref_name }}
            • Commit SHA: ${{ github.sha }}
            • Commit Author: ${{ steps.commit.outputs.author }}
            • Commit Message: ${{ steps.commit.outputs.message }}
            • Commit Description: ${{ steps.commit.outputs.body }}
            • Changes since ${{ steps.changelog.outputs.last_tag }}
            ${{ steps.changelog.outputs.changes }}
          files: |
            StarResonanceDpsAnalysis-${{ github.run_number }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
