<Window
    x:Class="StarResonanceDpsAnalysis.WPF.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behaviors="clr-namespace:StarResonanceDpsAnalysis.WPF.Behaviors"
    xmlns:ctrl="clr-namespace:StarResonanceDpsAnalysis.WPF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:loc="clr-namespace:StarResonanceDpsAnalysis.WPF.Properties"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:StarResonanceDpsAnalysis.WPF.ViewModels"
    Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Window_Settings_Title}}"
    Width="780"
    Height="690"
    MinWidth="660"
    MinHeight="540"
    d:DataContext="{d:DesignInstance Type=vms:SettingsDesignTimeViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="3000"
    lex:LocalizeDictionary.DesignCulture="zh-CN"
    lex:ResxLocalizationProvider.DefaultAssembly="StarResonanceDpsAnalysis.WPF"
    lex:ResxLocalizationProvider.DefaultDictionary="Resources"
    PreviewKeyDown="Window_PreviewKeyDown"
    Style="{StaticResource TransparentWindow}"
    mc:Ignorable="d">

    <!--  WindowChrome: 开启可拉伸边界  -->
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Window.Resources>

        <Style
            x:Key="SelectedIconfont"
            BasedOn="{StaticResource NormalTextBlockBaseStyle}"
            TargetType="TextBlock">
            <Setter Property="FontFamily" Value="pack://application:,,,/Assets/Fonts/#IconFont" />
            <Setter Property="Foreground" Value="#FFF" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        Color="#000"
                        Direction="0"
                        ShadowDepth="0"
                        Opacity="0.75"
                        BlurRadius="5" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingSectionListBorder" TargetType="Border">
            <Setter Property="Margin" Value="10" />
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="Background" Value="#FFF" />
        </Style>

        <Style x:Key="SettingSectionListTitleBorder" TargetType="Border">
            <Setter Property="Background" Value="#2297F4" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="CornerRadius" Value="5,5,0,0" />
        </Style>

        <Style
            x:Key="SettingSectionListTitle"
            BasedOn="{StaticResource NormalTextBlockBaseStyle}"
            TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFF" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        <Style x:Key="SectionTitleButton" TargetType="Button">
            <Setter Property="BorderBrush" Value="#EEE" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>

        <Style x:Key="SectionTitleBorder" TargetType="Border">
            <Setter Property="Padding" Value="10" />
        </Style>

        <Style
            x:Key="SectionListTitleText"
            BasedOn="{StaticResource NormalTextBlockBaseStyle}"
            TargetType="TextBlock" />

        <Style x:Key="SectionTitlePointer" TargetType="Border">
            <Setter Property="Width" Value="8" />
            <Setter Property="Height" Value="8" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="BorderBrush" Value="#000" />
            <Setter Property="BorderThickness" Value="0,1,1,0" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="45" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingDetailListBorder" TargetType="Border">
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="20,20,5,0" />
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="Background" Value="#FFF" />
        </Style>

        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="BorderBrush" Value="#E5E5E5" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Margin" Value="0,0,10,30" />
        </Style>


        <Style x:Key="SectionStackPanel" TargetType="StackPanel" />

        <!--
        "SectionFieldBorder" is the default style with a bottom border.
        "SectionFieldBorderBase" is the borderless field style.
        Use this style only when the field is the last one in a section to avoid double borders.
        
        "SectionFieldBorder" 为带有底部边框的默认样式
        "SectionFieldBorderBase" 为无边框的字段样式
        仅当该字段是当前分区中的最后一个时才使用, 以避免出现双重边框
        -->
        <Style x:Key="SectionFieldBorderBase" TargetType="Border">
            <Setter Property="Padding" Value="20,10,10,10" />
        </Style>

        <Style
            x:Key="SectionFieldBorder"
            BasedOn="{StaticResource SectionFieldBorderBase}"
            TargetType="Border">
            <Setter Property="BorderBrush" Value="#E5E5E5" />
            <Setter Property="BorderThickness" Value="0,0,0,2" />
        </Style>

        <Style
            x:Key="FieldName"
            BasedOn="{StaticResource NormalTextBlockBaseStyle}"
            TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style
            x:Key="FieldComboBox"
            BasedOn="{StaticResource AntComboBoxStyle}"
            TargetType="ComboBox">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style
            x:Key="AutoSelectButton"
            BasedOn="{StaticResource AntPrimaryButtonStyle}"
            TargetType="Button">
            <Setter Property="Padding" Value="15,0,15,0" />
        </Style>

        <Style
            x:Key="SuffixText"
            BasedOn="{StaticResource NormalTextBlockBaseStyle}"
            TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="ThemeButtonBorder" TargetType="Border">
            <Setter Property="BorderBrush" Value="#EEE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
        </Style>

        <Style x:Key="ThemeButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="5,0,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" Style="{StaticResource ThemeButtonBorder}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="ThemeButton_Active"
            BasedOn="{StaticResource ThemeButton}"
            TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border Background="{TemplateBinding Background}" Style="{StaticResource ThemeButtonBorder}">
                            <TextBlock Style="{StaticResource SelectedIconfont}" Text="&#xe64d;" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="ImageSelectButton"
            BasedOn="{StaticResource AntButtonStyle}"
            TargetType="Button">
            <Setter Property="Padding" Value="12,4,12,4" />
        </Style>

        <DataTemplate x:Key="LocalizedEnumTextBlockDataTemplate">
            <TextBlock Text="{Binding Display}" />
        </DataTemplate>

    </Window.Resources>

    <Border Background="#ECEBE9" Style="{StaticResource ShadowWindowBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ctrl:Header Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Window_Settings_Title}}" Grid.Row="0" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource SettingSectionListBorder}">
                    <StackPanel>

                        <Border Style="{StaticResource SettingSectionListTitleBorder}">
                            <TextBlock Style="{StaticResource SettingSectionListTitle}"
                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Window_Settings_Title}}" />
                        </Border>

                        <Button Click="Nav_Language_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Display_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_PlayerInfoCustomization_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfoCustomization_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_Basic_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Basic_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_Shortcut_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Shortcut_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_Character_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Character_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_Combat_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Combat_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_Theme_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Theme_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="Nav_ClassColors_Click" Style="{StaticResource SectionTitleButton}">
                            <Button.Template>
                                <ControlTemplate>
                                    <Border x:Name="bb" Style="{StaticResource SectionTitleBorder}">

                                        <Grid>
                                            <TextBlock Style="{StaticResource SectionListTitleText}"
                                                       Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_ClassColors_Title}}" />

                                            <Border Style="{StaticResource SectionTitlePointer}" />
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bb" Property="Background" Value="#EEE" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource SettingDetailListBorder}">
                    <ScrollViewer x:Name="ContentScrollViewer" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <ctrl:CardHeader
                                x:Name="SectionLanguage"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Display_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Language_Description}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Language_Title}}" />
                                            <ComboBox
                                                Grid.Column="2"
                                                ItemTemplate="{StaticResource LocalizedEnumTextBlockDataTemplate}"
                                                ItemsSource="{Binding AvailableLanguages}"
                                                SelectedItem="{Binding SelectedLanguage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource FieldComboBox}" />
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_MarqueeText}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_MarqueeText_Tooltip}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.EnableMarqueeText, Mode=TwoWay}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_Off}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_On}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>

                            <ctrl:CardHeader
                                x:Name="SectionPlayerInfoCustomization"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfoCustomization_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfoCustomization_Description}}" />
                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_UseCustomFormat}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.UseCustomFormat, Mode=TwoWay}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_Off}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_On}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_QuickPresets}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <WrapPanel>
                                                    <Button
                                                        Command="{Binding SetPlayerInfoFormatPresetCommand}"
                                                        CommandParameter="{}{Name} - {Spec} ({PowerLevel}-{SeasonStrength})"
                                                        Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_Default}}"
                                                        Margin="0,0,5,0"
                                                        Padding="10,4"
                                                        Style="{StaticResource AntButtonStyle}"
                                                        ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_Default_Tooltip}}" />
                                                    <Button
                                                        Command="{Binding SetPlayerInfoFormatPresetCommand}"
                                                        CommandParameter="{}{Name} ({PowerLevel})"
                                                        Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_Simple}}"
                                                        Margin="0,0,5,0"
                                                        Padding="10,4"
                                                        Style="{StaticResource AntButtonStyle}"
                                                        ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_Simple_Tooltip}}" />
                                                    <Button
                                                        Command="{Binding SetPlayerInfoFormatPresetCommand}"
                                                        CommandParameter="{}{Name} - {Spec} [S{SeasonStrength} Lv{SeasonLevel}] ({PowerLevel})"
                                                        Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_Detailed}}"
                                                        Margin="0,0,5,0"
                                                        Padding="10,4"
                                                        Style="{StaticResource AntButtonStyle}"
                                                        ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_Detailed_Tooltip}}" />
                                                    <Button
                                                        Command="{Binding SetPlayerInfoFormatPresetCommand}"
                                                        CommandParameter="{}{Name}"
                                                        Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_NameOnly}}"
                                                        Padding="10,4"
                                                        Style="{StaticResource AntButtonStyle}"
                                                        ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preset_NameOnly_Tooltip}}" />
                                                </WrapPanel>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_AddFields}}"
                                                VerticalAlignment="Top"
                                                Margin="0,8,0,0" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ItemsControl
                                                    IsEnabled="{Binding AppConfig.UseCustomFormat}"
                                                    ItemsSource="{Binding AvailableFormatFields}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Button
                                                                Command="{Binding DataContext.AddFieldToFormatCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"
                                                                Margin="0,0,5,5"
                                                                Padding="8,4"
                                                                Style="{StaticResource AntButtonStyle}"
                                                                ToolTip="{Binding Example}">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock
                                                                        FontWeight="Bold"
                                                                        Margin="0,0,4,0"
                                                                        Text="+" />
                                                                    <TextBlock Text="{Binding DisplayName}" />
                                                                </StackPanel>
                                                            </Button>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_AddSeparators}}"
                                                VerticalAlignment="Top"
                                                Margin="0,8,0,0" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <WrapPanel IsEnabled="{Binding AppConfig.UseCustomFormat}">
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=" - "
                                                        Content="-"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=" | "
                                                        Content="|"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=" / "
                                                        Content="/"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=" • "
                                                        Content="•"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=", "
                                                        Content=", "
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=" "
                                                        Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Separator_Space}}"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter="("
                                                        Content="( "
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter=")"
                                                        Content=") "
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter="[
"
                                                        Content="[
"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                    <Button
                                                        Command="{Binding AddSeparatorToFormatCommand}"
                                                        CommandParameter="]"
                                                        Content="]"
                                                        Margin="0,0,5,5"
                                                        Padding="8,4"
                                                        Style="{StaticResource AntButtonStyle}" />
                                                </WrapPanel>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_FormatString}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_FormatString_Tooltip}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBox
                                                    Grid.Column="0"
                                                    IsEnabled="{Binding AppConfig.UseCustomFormat}"
                                                    MinHeight="32"
                                                    Style="{StaticResource AntTextBoxStyle}"
                                                    Text="{Binding AppConfig.PlayerInfoFormatString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    TextWrapping="Wrap"
                                                    VerticalContentAlignment="Center" />
                                                <Button
                                                    Grid.Column="2"
                                                    Command="{Binding ClearFormatCommand}"
                                                    Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Clear}}"
                                                    Padding="12,4"
                                                    Style="{StaticResource AntButtonStyle}"
                                                    ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Clear_Tooltip}}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerInfo_Preview}}"
                                                VerticalAlignment="Top"
                                                Margin="0,8,0,0" />
                                            <Border
                                                Grid.Column="2"
                                                Background="#F8F9FA"
                                                BorderBrush="#DEE2E6"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                Padding="10">
                                                <TextBlock
                                                    FontSize="13"
                                                    FontWeight="Medium"
                                                    Text="{Binding FormatPreview}"
                                                    TextWrapping="Wrap" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>
                            <ctrl:CardHeader
                                x:Name="SectionBasic"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Basic_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Basic_Sub}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_NetworkAdapter_Label}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <ComboBox
                                                    Grid.Column="0"
                                                    DisplayMemberPath="Description"
                                                    ItemsSource="{Binding AvailableNetworkAdapters, UpdateSourceTrigger=PropertyChanged}"
                                                    SelectedItem="{Binding AppConfig.PreferredNetworkAdapter, Mode=TwoWay}"
                                                    SelectedValuePath="Description"
                                                    Style="{StaticResource AntComboBoxStyle}" />
                                                <Button
                                                    Grid.Column="2"
                                                    Command="{Binding NetworkAdapterAutoSelectCommand}"
                                                    Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_NetworkAdapterAutoSelect_Button}}"
                                                    Style="{StaticResource AutoSelectButton}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>

                            <ctrl:CardHeader
                                x:Name="SectionShortcut"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Shortcut_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Shortcut_Sub}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Shortcut_MouseThrough}}" />
                                            <TextBox
                                                x:Name="MouseThroughTextBox"
                                                Grid.Column="2"
                                                IsReadOnly="True"
                                                Style="{StaticResource AntTextBoxStyle}"
                                                Text="{Binding AppConfig.MouseThroughShortcut, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource KeyToStringConverter}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Shortcut_MouseThrough_Tooltip}}">
                                                <b:Interaction.Behaviors>
                                                    <behaviors:KeyDownCommandBehavior
                                                        Command="{Binding HandleMouseThroughShortcutCommand}" />
                                                </b:Interaction.Behaviors>
                                            </TextBox>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Shortcut_ClearData}}" />
                                            <TextBox
                                                x:Name="ClearDataTextBox"
                                                Grid.Column="2"
                                                IsReadOnly="True"
                                                Style="{StaticResource AntTextBoxStyle}"
                                                Text="{Binding AppConfig.ClearDataShortcut, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource KeyToStringConverter}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Shortcut_ClearData_Tooltip}}">
                                                <b:Interaction.Behaviors>
                                                    <behaviors:KeyDownCommandBehavior
                                                        Command="{Binding HandleClearDataShortcutCommand}" />
                                                </b:Interaction.Behaviors>
                                            </TextBox>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Shortcut_TopMost}}" />
                                            <TextBox
                                                x:Name="TopMostTextBox"
                                                Grid.Column="2"
                                                IsReadOnly="True"
                                                Style="{StaticResource AntTextBoxStyle}"
                                                Text="{Binding AppConfig.TopmostShortcut, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource KeyToStringConverter}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Shortcut_TopMost_Tooltip}}">
                                                <b:Interaction.Behaviors>
                                                    <behaviors:KeyDownCommandBehavior
                                                        Command="{Binding HandleTopMostShortcutCommand}" />
                                                </b:Interaction.Behaviors>
                                            </TextBox>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>

                            <!--  角色设置区域  -->
                            <ctrl:CardHeader
                                x:Name="SectionCharacter"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Character_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Character_Sub}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Character_Uid}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>


                                                <TextBox
                                                    Style="{StaticResource AntTextBoxStyle}"
                                                    Text="{Binding AppConfig.Uid, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Character_Uid_Tooltip}}" />
                                                <Button
                                                    Grid.Column="2"
                                                    Command="{Binding TryGetCurrentUidCommand}"
                                                    Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_PlayerUid_Get}}"
                                                    Style="{StaticResource AutoSelectButton}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>

                            <ctrl:CardHeader
                                x:Name="SectionCombat"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Combat_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Combat_Sub}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_ClearAfterBattle}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBox
                                                    Grid.Column="0"
                                                    Style="{StaticResource AntTextBoxStyle}"
                                                    Text="{Binding AppConfig.CombatTimeClearDelay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Common_Seconds}}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="75*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_ClearAfterTeleport}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.ClearLogAfterTeleport, Mode=TwoWay}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_Off}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_On}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateMode}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateMode_Tooltip}}"
                                                Grid.ColumnSpan="3" Margin="0,0,260,0" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.DpsUpdateMode, Mode=TwoWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Active}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateMode_Passive}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateMode_Active}}"
                                                    ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateMode_Tooltip}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateInterval}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_DpsUpdateInterval_Tooltip}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="35" />
                                                    <ColumnDefinition Width="5" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <Slider
                                                    Grid.Column="0"
                                                    IsEnabled="{Binding AppConfig.DpsUpdateMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Active}"
                                                    IsSnapToTickEnabled="True"
                                                    Maximum="5000"
                                                    Minimum="100"
                                                    Style="{StaticResource AntSliderStyle}"
                                                    TickFrequency="100"
                                                    Value="{Binding AppConfig.DpsUpdateInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{Binding AppConfig.DpsUpdateInterval, Mode=OneWay}" />
                                                <TextBlock
                                                    Grid.Column="4"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Common_Milliseconds}}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_NumberMode}}" />
                                            <ComboBox
                                                Grid.Column="2"
                                                ItemTemplate="{StaticResource LocalizedEnumTextBlockDataTemplate}"
                                                ItemsSource="{Binding AvailableNumberDisplayModes}"
                                                SelectedItem="{Binding SelectedNumberDisplayMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource FieldComboBox}" />
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_ShowDamage}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.ShowDamage, Mode=TwoWay}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_Off}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_On}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_ShowDps}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.ShowDps, Mode=TwoWay}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_Off}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_On}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_ShowPercentage}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.ShowPercentage, Mode=TwoWay}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_Off}}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Switch_On}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_DpsMode}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_DpsMode_Tooltip}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <ctrl:SwitchControl
                                                    IsChecked="{Binding AppConfig.UseConverterBasedDps, Mode=TwoWay}"
                                                    OnContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_DpsMode_On}}"
                                                    OffContent="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_DpsMode_Off}}"
                                                    ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_DpsMode_Switch_Tooltip}}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_Opacity}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="35" />
                                                </Grid.ColumnDefinitions>

                                                <Slider
                                                    Grid.Column="0"
                                                    IsSnapToTickEnabled="True"
                                                    Maximum="95"
                                                    Minimum="5"
                                                    Style="{StaticResource AntSliderStyle}"
                                                    TickFrequency="1"
                                                    Value="{Binding AppConfig.Opacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{Binding AppConfig.Opacity, Mode=OneWay}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_MaxHistoryCount}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Combat_MaxHistoryCount_Tooltip}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="35" />
                                                    <ColumnDefinition Width="5" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <Slider
                                                    Grid.Column="0"
                                                    IsSnapToTickEnabled="True"
                                                    Maximum="50"
                                                    Minimum="5"
                                                    Style="{StaticResource AntSliderStyle}"
                                                    TickFrequency="5"
                                                    Value="{Binding AppConfig.MaxHistoryCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{Binding AppConfig.MaxHistoryCount, Mode=OneWay}" />
                                                <TextBlock
                                                    Grid.Column="4"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Common_Records}}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_TimeSeriesCapacity}}"
                                                ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Display_TimeSeriesCapacity_Tooltip}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="50" />
                                                    <ColumnDefinition Width="5" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <Slider
                                                    Grid.Column="0"
                                                    IsSnapToTickEnabled="True"
                                                    Maximum="1000"
                                                    Minimum="50"
                                                    Style="{StaticResource AntSliderStyle}"
                                                    TickFrequency="50"
                                                    Value="{Binding AppConfig.TimeSeriesSampleCapacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{Binding AppConfig.TimeSeriesSampleCapacity, Mode=OneWay}" />
                                                <TextBlock
                                                    Grid.Column="4"
                                                    Style="{StaticResource SuffixText}"
                                                    Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Common_Samples}}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>

                            <ctrl:CardHeader
                                x:Name="SectionTheme"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Theme_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_Theme_Description}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <Border Style="{StaticResource SectionFieldBorder}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Theme_WindowColor}}" />
                                            <Border Grid.Column="2" HorizontalAlignment="Right">
                                                <StackPanel Orientation="Horizontal">
                                                    <WrapPanel>
                                                        <Button Background="#1690F8"
                                                                Command="{Binding SetThemeColorCommand}"
                                                                CommandParameter="#1690F8"
                                                                Style="{StaticResource ThemeButton}">
                                                            <Button.Template>
                                                                <ControlTemplate>
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            Style="{StaticResource ThemeButtonBorder}">
                                                                        <TextBlock
                                                                            Style="{StaticResource SelectedIconfont}"
                                                                            Text="&#xe64d;"
                                                                            Visibility="{Binding AppConfig.ThemeColor, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=#1690F8}" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                        <Button Background="#FFA9A8"
                                                                Command="{Binding SetThemeColorCommand}"
                                                                CommandParameter="#FFA9A8"
                                                                Style="{StaticResource ThemeButton}">
                                                            <Button.Template>
                                                                <ControlTemplate>
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            Style="{StaticResource ThemeButtonBorder}">
                                                                        <TextBlock
                                                                            Style="{StaticResource SelectedIconfont}"
                                                                            Text="&#xe64d;"
                                                                            Visibility="{Binding AppConfig.ThemeColor, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=#FFA9A8}" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                        <Button Background="#F7A0DD"
                                                                Command="{Binding SetThemeColorCommand}"
                                                                CommandParameter="#F7A0DD"
                                                                Style="{StaticResource ThemeButton}">
                                                            <Button.Template>
                                                                <ControlTemplate>
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            Style="{StaticResource ThemeButtonBorder}">
                                                                        <TextBlock
                                                                            Style="{StaticResource SelectedIconfont}"
                                                                            Text="&#xe64d;"
                                                                            Visibility="{Binding AppConfig.ThemeColor, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=#F7A0DD}" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                        <Button Background="#2F2F2F"
                                                                Command="{Binding SetThemeColorCommand}"
                                                                CommandParameter="#2F2F2F"
                                                                Style="{StaticResource ThemeButton}">
                                                            <Button.Template>
                                                                <ControlTemplate>
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            Style="{StaticResource ThemeButtonBorder}">
                                                                        <TextBlock
                                                                            Style="{StaticResource SelectedIconfont}"
                                                                            Text="&#xe64d;"
                                                                            Visibility="{Binding AppConfig.ThemeColor, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=#2F2F2F}" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                        <Button Background="#D0D0D0"
                                                                Command="{Binding SetThemeColorCommand}"
                                                                CommandParameter="#D0D0D0"
                                                                Style="{StaticResource ThemeButton}">
                                                            <Button.Template>
                                                                <ControlTemplate>
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            Style="{StaticResource ThemeButtonBorder}">
                                                                        <TextBlock
                                                                            Style="{StaticResource SelectedIconfont}"
                                                                            Text="&#xe64d;"
                                                                            Visibility="{Binding AppConfig.ThemeColor, Converter={StaticResource EqualityToVisibilityConverter}, ConverterParameter=#D0D0D0}" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>
                                                        </Button>
                                                    </WrapPanel>

                                                    <!-- ⭐ 新增：自定义颜色按钮（多彩图标） -->
                                                    <Button x:Name="CustomColorButton" 
                                                            Width="30" Height="30"
                                                            Margin="5,0,0,0"
                                                            BorderBrush="#EEE" BorderThickness="1"
                                                            ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Theme_CustomColor_Tooltip}}">
                                                        <Button.Background>
                                                            <LinearGradientBrush StartPoint="0,0.25" EndPoint="1,0.75">
                                                                <GradientStop Color="#F00" Offset="0.0"/>
                                                                <GradientStop Color="#F80" Offset="0.1"/>
                                                                <GradientStop Color="#FF0" Offset="0.25"/>
                                                                <GradientStop Color="#8F0" Offset="0.42"/>
                                                                <GradientStop Color="#0F0" Offset="0.44"/>
                                                                <GradientStop Color="#0F8" Offset="0.48"/>
                                                                <GradientStop Color="#0FF" Offset="0.58"/>
                                                                <GradientStop Color="#08F" Offset="0.68"/>
                                                                <GradientStop Color="#00F" Offset="0.78"/>
                                                                <GradientStop Color="#80F" Offset="0.85"/>
                                                                <GradientStop Color="#F0F" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Button.Background>
                                                        <Button.Template>
                                                            <ControlTemplate>
                                                                <Border Background="{TemplateBinding Background}"
                                                                        Style="{StaticResource ThemeButtonBorder}">
                                                                    <TextBlock Style="{StaticResource SelectedIconfont}"
                                                                               Text="&#xe6ad;"
                                                                               Foreground="White"
                                                                               FontSize="16"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                        <Button.Triggers>
                                                            <EventTrigger RoutedEvent="Button.Click">
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <BooleanAnimationUsingKeyFrames 
                                                                            Storyboard.TargetName="ColorPickerPopup"
                                                                            Storyboard.TargetProperty="IsOpen">
                                                                            <DiscreteBooleanKeyFrame KeyTime="0" Value="True"/>
                                                                        </BooleanAnimationUsingKeyFrames>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </EventTrigger>
                                                        </Button.Triggers>
                                                    </Button>

                                                    <!-- ⭐ 颜色选择器 Popup -->
                                                    <Popup x:Name="ColorPickerPopup"
                                                           PlacementTarget="{Binding ElementName=CustomColorButton}"
                                                           Placement="Bottom"
                                                           StaysOpen="False"
                                                           AllowsTransparency="True">
                                                        <Border Background="White" 
                                                                BorderBrush="#999" 
                                                                BorderThickness="1"
                                                                CornerRadius="8">
                                                            <Border.Effect>
                                                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                                                            </Border.Effect>
                                                            <ctrl:SimpleColorPicker 
                                                                SelectedColor="{Binding CurrentThemeColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                Margin="5"/>
                                                        </Border>
                                                    </Popup>
                                                </StackPanel>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <Border Style="{StaticResource SectionFieldBorderBase}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="39*" />
                                                <ColumnDefinition Width="*" MinWidth="10" />
                                                <ColumnDefinition Width="60*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                Grid.Column="0"
                                                Style="{StaticResource FieldName}"
                                                Text="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Theme_BackgroundImage}}" />
                                            <Grid Grid.Column="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="5" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBox
                                                    Grid.Column="0"
                                                    IsReadOnly="True"
                                                    Style="{StaticResource AntTextBoxStyle}"
                                                    Text="{Binding AppConfig.BackgroundImagePath, Mode=OneWay}"
                                                    ToolTip="{Binding AppConfig.BackgroundImagePath}" />
                                                <Button
                                                    Grid.Column="2"
                                                    Command="{Binding SelectBackgroundImageCommand}"
                                                    Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Theme_SelectImage}}"
                                                    Padding="12,4"
                                                    Style="{StaticResource AutoSelectButton}" />
                                                <Button
                                                    Grid.Column="4"
                                                    Command="{Binding ClearBackgroundImageCommand}"
                                                    Content="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Theme_ClearImage}}"
                                                    Padding="12,4"
                                                    Style="{StaticResource AntButtonStyle}" />
                                            </Grid>
                                        </Grid>
                                    </Border>

                                </StackPanel>
                            </Border>

                            <!-- Class Color Section -->
                            <ctrl:CardHeader 
                                x:Name="SectionClassColors"
                                Title="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_ClassColors_Title}}"
                                Subtitle="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_Section_ClassColors_Description}}" />

                            <Border Style="{StaticResource SectionBorder}">
                                <StackPanel Style="{StaticResource SectionStackPanel}">

                                    <ItemsControl ItemsSource="{Binding ClassColorSettings}" Focusable="False">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource SectionFieldBorder}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="39*" />
                                                            <ColumnDefinition Width="*" MinWidth="10" />
                                                            <ColumnDefinition Width="60*" />
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                            <Image Source="{Binding Class, Converter={StaticResource ClassesToColoredIconConverter}}" 
                                                                   Width="26" Height="26" Margin="0,0,10,0"
                                                                   RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True" />
                                                            <TextBlock Style="{StaticResource FieldName}" Text="{lex:Loc {Binding Name, StringFormat={}Classes_{0}}}"></TextBlock>
                                                        </StackPanel>


                                                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                                            <!-- Color Toggle Button -->
                                                            <ToggleButton x:Name="ColorToggle" Width="30" Height="30" Margin="0,0,10,0" Style="{StaticResource AntToggleButtonStyle}" Padding="0">
                                                                <ToggleButton.Content>

                                                                    <Border Width="24" Height="24" CornerRadius="4">
                                                                        <Border.Background>
                                                                            <SolidColorBrush Color="{Binding Color}" />
                                                                        </Border.Background>
                                                                    </Border>
                                                                </ToggleButton.Content>
                                                            </ToggleButton>

                                                            <Popup IsOpen="{Binding IsChecked, ElementName=ColorToggle}" 
                                                                   PlacementTarget="{Binding ElementName=ColorToggle}" 
                                                                   Placement="Bottom" 
                                                                   StaysOpen="False" 
                                                                   AllowsTransparency="True">
                                                                <Border Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Margin="5">
                                                                    <Border.Effect>
                                                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                                                                    </Border.Effect>
                                                                    <ctrl:SimpleColorPicker SelectedColor="{Binding Color, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10"/>
                                                                </Border>
                                                            </Popup>

                                                            <Button Command="{Binding ResetCommand}" Padding="8,4" 
                                                                    FontFamily="pack://application:,,,/Assets/Fonts/#IconFont" 
                                                                    Content="{lex:Loc {x:Static loc:ResourcesKeys.Settings_ClassColors_Reset}}" 
                                                                    ToolTip="{lex:Loc Key={x:Static loc:ResourcesKeys.Settings_ClassColors_Reset_Tooltip}}" 
                                                                    Style="{StaticResource AntButtonStyle}"/>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

            </Grid>

            <ctrl:Footer
                Grid.Row="2"
                Height="50"
                CancelClick="Footer_CancelClick"
                CancelCommand="{Binding CancelCommand}"
                ConfirmClick="Footer_ConfirmClick"
                ConfirmCommand="{Binding ConfirmCommand}" />
        </Grid>
    </Border>
</Window>
